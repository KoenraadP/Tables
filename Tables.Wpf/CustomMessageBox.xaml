<Window x:Class="Tables.Wpf.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nog eens?" Height="124" Width="300" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageTextBlock" Grid.Row="0" Margin="10" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button x:Name="YesButton" Content="Ja" Width="75" Click="YesButton_Click" IsEnabled="False"/>
            <Button x:Name="NoButton" Content="Nee" Width="75" Click="NoButton_Click" Margin="10,0,0,0" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>
